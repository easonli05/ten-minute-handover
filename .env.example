# Neon Postgres connection string (pooled). Get this from the Neon dashboard,
# or let Vercel's Neon integration inject it automatically on deploy.
DATABASE_URL=

# Any long random string. This is the single passcode used to unlock the app.
APP_PASSCODE=
